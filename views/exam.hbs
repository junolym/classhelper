<!DOCTYPE html>
<html lang="en">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0"/>

    <title>{{title}}</title>

    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/font-awesome/css/font-awesome.min.css" rel="stylesheet" >
    <link href="/stylesheets/index.css" rel="stylesheet">
</head>
<body>
    <section>
      <h1>{{examname}}</h1>
    </section>
    <!--试题详情-->
    <section>
      <ol>
        {{#each exam}}
        {{#if question_selection}}
        <li class="eachdescription">{{description}}</br>
          {{#each selectionSet}}
          <input type="checkbox" {{lookup ../answer @index}} /><label class="eachselection">{{this}}</label><br/>
          {{/each}}
        </li>
        {{else if question_judgeanswer}}
        <li class="eachdescription">{{description}}<br>
          <input type="radio" name="{{@index}}" value="1" {{answer_right}}>正确<br>
          <input type="radio" name="{{@index}}" value="0" {{answer_wrong}}>错误
        </li>
        {{else}}
        <li class="eachdescription">{{description}}</br>
          <textarea rows="3" cols="30" overflow-y="scroll">{{answer}}</textarea>
        </li>
        {{/if}}
        {{/each}}
      </ol>
    </section>

    <script src="https://cdn.bootcss.com/jquery/1.11.0/jquery.min.js" type="text/javascript"></script>
    <script>window.jQuery || document.write('<script src="js/jquery-1.11.0.min.js"><\/script>')</script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
    <script src="/javascripts/exam.js"></script>
</body>
</html>
